// =====================================================\n// إعدادات قاعدة البيانات\n// =====================================================\n\nconst mysql = require('mysql2/promise');\n\n// إعدادات الاتصال\nconst pool = mysql.createPool({\n  host: 'localhost',\n  user: 'root',\n  password: '', // غير كلمة المرور إذا لزم الأمر\n  database: 'aam_system',\n  waitForConnections: true,\n  connectionLimit: 10,\n  queueLimit: 0\n});\n\nmodule.exports = pool;\n